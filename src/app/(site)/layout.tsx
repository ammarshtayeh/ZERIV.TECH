/**
 * Studio pages share the experience design system via PageShell.
 * Legacy RTL chrome is retired — routes remain under (site) for path stability.
 */
export default function SiteLayout({ children }: { children: React.ReactNode }) {
  return children;
}
